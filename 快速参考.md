# 🚀 AI 代码生成引擎 - 快速参考

## 📌 核心问题快速回答

### Q1: 是否需要将组件模板代码迁移到 MCP 工具？

```
✅ 强烈建议迁移

原因：
1. 代码可用度：70% → 80%+ ✨
2. 规范文档：3700+ 行 → 2000 行
3. AI 学习：通过 MCP 精确注入示例代码
```

### Q2: 迁移后如何使用？

```
🎯 方式1：自动检索（推荐）⭐

用户输入：
"做一个员工列表页，支持姓名搜索、新增、编辑、删除"

AI 自动完成：
1. match_template → 自动匹配模板
2. build_prompt → 自动附加示例代码和知识图谱
3. 生成代码 → 80% 可用度 ✨

--------------------------------------------------

🎯 方式2：明确指定模板（可选）

用户输入：
"react-nonstandard-detail 做一个出差申请单详情页"

AI 自动识别：
- 提取模板ID → 跳过匹配 → 直接使用
```

### Q3: 调用 MCP 时能否不指定具体方法自动检索？

```
✅ 已实现！

核心算法：智能关键词打分

scoreTemplate(text, template):
  - 正向关键词：每匹配一个 +2 分
  - 反向关键词：每匹配一个 -3 分
  - 场景加权：特定场景额外 +3 分
  
→ 自动选择得分最高的模板
→ 自动附加增强上下文（示例代码 + 知识图谱）
```

---

## 🎯 使用流程（3 步）

### Step 1: 启动 MCP 服务

```bash
cd codegen-engine
npm install
npm run start:http
```

### Step 2: 配置通义灵码

```
设置 → MCP 服务 → 添加：
- 类型：Streamable HTTP
- 地址：http://127.0.0.1:7331/mcp
```

### Step 3: 开始生成代码

```
输入：做一个员工列表页，支持姓名搜索、新增、编辑、删除
→ AI 自动匹配模板 → 自动注入示例代码 → 生成高质量代码
```

---

## 📁 当前示例代码库

```
templates/examples/
├── react-standard-list/          ✅ React 标准列表页
│   ├── sample.md
│   ├── hooks.example.ts
│   ├── index.example.tsx
│   ├── types.example.ts
│   └── components/
│       └── EditModal.example.tsx
│
├── react-nonstandard-detail/     ✅ React 非标详情页
│   ├── sample.md
│   ├── hooks/
│   │   ├── index.ts
│   │   └── useDetailData.example.ts
│   ├── index.example.tsx
│   └── index.example.less
│
└── react-import-modal/           ✅ React 导入弹窗
    ├── sample.md
    ├── index.example.tsx
    └── index.example.less
```

---

## 🔧 可用 MCP 工具

### 1. list_templates
```typescript
// 列出所有可用模板
AI 调用：list_templates()
返回：[{ id, name, scenes, keywords }]
```

### 2. match_template
```typescript
// 根据需求匹配模板
AI 调用：match_template({ text: "做一个员工列表页..." })
返回：{ chosen: { id: "react-standard-list-crud", score: 8 } }
```

### 3. build_prompt 🆕 增强版
```typescript
// 构建增强版提示词（自动附加示例代码）
AI 调用：build_prompt({ text: "做一个员工列表页..." })
返回：{
  templateId: "react-standard-list-crud",
  prompt: "任务标准：...",
  enhancedContext: {
    componentKnowledge: { "ant-design-pro": "..." },
    codeExamples: { 
      "hooks.example.ts": "...",
      "index.example.tsx": "..."
    }
  }
}
```

### 4. get_component_knowledge
```typescript
// 获取组件库知识图谱
AI 调用：get_component_knowledge({ scope: "common" })
返回：{ "ant-design-pro": "...", "element-plus": "..." }
```

### 5. get_code_examples
```typescript
// 获取模板示例代码
AI 调用：get_code_examples({ templateId: "react-standard-list-crud" })
返回：{ "hooks.example.ts": "...", "index.example.tsx": "..." }
```

---

## 📊 效果对比

```
┌────────────────────┬─────────┬──────────┬──────────┐
│      方案          │ 可用度  │ 使用成本 │ 维护成本 │
├────────────────────┼─────────┼──────────┼──────────┤
│ 纯提示词           │  40%    │   高     │   低     │
│ 提示词 + Rule      │  70%    │   中     │   中     │
│ MCP + 示例代码 ✨  │  80%+   │   低     │   中     │
└────────────────────┴─────────┴──────────┴──────────┘
```

---

## 🎯 最佳实践

### ✅ DO

```
1. 保持规范文档精简（专注规则）
2. 示例代码独立管理（templates/examples/）
3. 组件库知识持续更新（knowledge/common/）
4. 定期从项目中提取优秀代码作为示例
```

### ❌ DON'T

```
1. 不要在规范文档中混入完整模板代码
2. 不要跳过 MCP 自动匹配，手动复制模板
3. 不要忽视示例代码的维护和更新
```

---

## 🚀 下一步行动

### 立即可做 ✅

- [x] 启动 MCP 服务：`npm run start:http`
- [x] 配置通义灵码
- [x] 测试生成代码

### 后续优化 ⚠️

- [ ] 补充 Vue 3 示例代码
- [ ] 精简规范文档（删除已迁移的模板代码）
- [ ] 添加更多组件库知识

---

## 📖 相关文档

| 文档 | 说明 |
|-----|------|
| [使用指南.md](./使用指南.md) | 完整的使用指南和流程说明 |
| [README.md](./README.md) | MCP 服务详细文档 |
| [迁移方案.md](./迁移方案.md) | 模板代码迁移计划 |

---

## 💡 关键要点

```
1. 迁移 = 代码可用度提升 10%
2. 自动检索 = 使用成本降低
3. 示例代码 = AI 学习更准确
4. 团队协作 = 知识共享增强
```

---

**🎉 立即开始使用，让 AI 为您生成高质量代码！**
