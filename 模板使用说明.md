# 组件模板使用说明

## 📋 概述

本 MCP 服务提供了前端组件模板，通过 `codegen-engine` 工具自动匹配和获取。所有模板代码已从规则文件中抽离，实现**规则专注于规则，模板由 MCP 提供**。

## 🎯 可用模板列表

### React 模板

| 模板ID | 模板名称 | 适用场景 | 关键词 |
|--------|---------|---------|--------|
| `react-standard-list-crud` | React 标准列表页 | ProTable + 搜索 + 新增/编辑弹窗 | 列表、表格、搜索、CRUD、增删改查 |
| `react-import-modal` | React 导入弹窗 | Excel 导入 + 异步/同步 + 导入记录 | 导入、上传、excel、xlsx、模板下载 |
| `react-nonstandard-detail` | React 非标独立详情页 | 审批流程/附件预览/独立路由 | 详情、单据详情、审批、流程、附件预览 |

### Vue 3 模板

| 模板ID | 模板名称 | 适用场景 | 关键词 |
|--------|---------|---------|--------|
| `vue3-standard-list-crud` | Vue 3 标准列表页 | Element Plus + Composition API + 搜索 + 新增/编辑弹窗 | 列表、表格、vue3、composition、el-table |

### Vue 2 模板

| 模板ID | 模板名称 | 适用场景 | 关键词 |
|--------|---------|---------|--------|
| `vue2-standard-list-crud` | Vue 2 标准列表页 | Element UI + Options API + 搜索 + 新增/编辑弹窗 | 列表、表格、vue2、options、getwayTable |

## 🚀 使用方式

### 方式一：自动匹配（推荐）

直接在 AI 对话中使用规范 + 提示词，MCP 工具会根据关键词自动匹配最合适的模板：

```
任务标准：ai-fe-code-std.md 为标准执行任务

一句话需求：做一个员工列表页，支持姓名/工号查询，新增编辑弹窗，支持删除和批量导出

文件夹名称: employee-list

接口数据结构：{
  "name": "姓名",
  "employeeNo": "工号",
  "department": "部门",
  "status": "状态"
}

页面需求：
- 搜索表单：姓名、工号、部门、状态
- 数据表格：序号 + 基本信息 + 操作列（编辑/删除）
- 编辑弹窗：新增/编辑共用，提交成功刷新表格
- 批量操作：批量删除、批量导出
```

### 方式二：手动指定模板

如果需要使用特定模板，可以明确指定：

```
任务标准：ai-fe-code-std.md 为标准执行任务

使用模板：react-import-modal

一句话需求：做一个excel导入弹窗，支持模板下载、上传、导入记录和下载错误文件

接口及数据结构：
- 模板下载：downloadTemplate
- 上传：uploadExcelDataAsync
- 导入记录：getBatchFileList
```

### 方式三：通过 MCP 工具直接获取

在支持 MCP 的 AI 工具中（如通义灵码），可以直接调用：

```
调用 mcp 工具 codegen-engine 执行任务：
任务标准：ai-fe-code-std.md 为标准执行任务
使用模板：vue3-standard-list-crud
文件夹名称: user-list
...
```

## 📂 模板文件结构

### React 标准列表页

```
react-standard-list/
├── index.example.tsx           # 主页面（ProTable + 搜索）
├── components/
│   └── EditModal.example.tsx  # 编辑弹窗（ModalForm）
├── hooks.example.ts            # 自定义 Hook（可选）
├── types.example.ts            # TypeScript 类型定义
└── sample.md                   # 使用说明
```

### Vue 3 标准列表页

```
vue3-standard-list/
├── index.example.vue               # 主页面（el-table + 搜索）
├── components/
│   └── EditDialog.example.vue     # 编辑弹窗（el-dialog）
├── composables.example.ts          # Composable（可选）
├── types.example.ts                # TypeScript 类型定义
└── sample.md                       # 使用说明
```

### Vue 2 标准列表页

```
vue2-standard-list/
├── index.example.vue               # 主页面（getwayTable + 搜索）
├── components/
│   └── EditDialog.example.vue     # 编辑弹窗（el-dialog）
└── sample.md                       # 使用说明
```

## 🔍 模板匹配规则

MCP 工具会根据以下规则自动匹配模板：

1. **关键词匹配**：根据用户需求中的关键词（如"列表"、"导入"、"详情"）匹配
2. **技术栈识别**：自动识别 React/Vue 3/Vue 2
3. **场景判断**：标准列表/导入/详情/表单等场景
4. **排除规则**：通过反关键词避免错误匹配

### 匹配示例

| 用户需求 | 匹配模板 | 原因 |
|---------|---------|------|
| 做一个员工列表页，支持搜索和编辑 | `react-standard-list-crud` | 包含"列表"、"搜索"、"编辑"关键词 |
| 做一个excel导入弹窗 | `react-import-modal` | 包含"导入"、"excel"关键词 |
| 做一个出差申请单详情页，包含审批流程 | `react-nonstandard-detail` | 包含"详情"、"审批流程"关键词 |
| Vue 3 项目，做一个用户管理列表 | `vue3-standard-list-crud` | 明确 Vue 3 + 包含"列表"关键词 |

## ⚠️ 注意事项

### 1. TypeScript 类型引入规则

生成代码前，MCP 工具会**自动检查**项目中的全局类型声明：

- ✅ **全局类型**（在 `types/global.d.ts` 中）：**不引入**，直接使用
- ✅ **第三方库类型**（如 `ProColumns`、`FormInstance`）：**必须引入**
- ✅ **局部类型**（当前模块 `types.ts`）：从 `./types` 引入

### 2. 生成文件顺序

对于非标独立页面，**必须严格按以下顺序生成**：

1. **hooks/composables 文件**（最高优先级）
2. hooks 导出文件（`index.ts`）
3. 组件主文件（`index.tsx` / `index.vue`）
4. 样式文件（`index.less`）

### 3. 代码自检

生成完成后，**必须执行自检**：

- ✅ TypeScript 类型检查
- ✅ ESLint 规则检查
- ✅ 代码规范检查
- ✅ 功能完整性检查

## 📖 参考文档

- **AI 前端代码生成规范**：`ai-fe-code-std.md`
- **使用指南**：`codegen-engine/使用指南.md`
- **快速参考**：`codegen-engine/快速参考.md`
- **实际使用案例**：`实际在通义灵码里面使用方式.md`

## 🛠️ 模板扩展

如需添加新模板：

1. 在 `templates/examples/` 目录下创建模板文件夹
2. 创建 `.example.tsx`/`.example.vue` 等模板文件
3. 创建 `sample.md` 说明文档
4. 在 `template-registry.json` 中注册模板
5. 重启 MCP 服务

## 💡 最佳实践

1. **优先使用自动匹配**：让 MCP 工具根据关键词自动选择最合适的模板
2. **提供完整信息**：接口数据结构、页面需求、业务规则等信息越完整，生成效果越好
3. **遵循规范**：始终以 `ai-fe-code-std.md` 为标准执行任务
4. **检查生成结果**：关注自检报告，确保代码质量

## 📞 技术支持

如有问题或需要帮助，请参考：
- 规范文档：`ai-fe-code-std.md`
- 使用指南：`codegen-engine/使用指南.md`
- 示例模板：`codegen-engine/templates/examples/`
